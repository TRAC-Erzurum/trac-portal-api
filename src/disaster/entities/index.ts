import { Disaster } from './disaster.entity';
import { DisasterMembership } from './disaster-membership.entity';
import { Observation } from './observation.entity';
import { ObservationFeedback } from './observation-feedback.entity';
import { ObservationPhoto } from './observation-photo.entity';
import { DisasterSetting } from './disaster-setting.entity';

export const entities = [
  Disaster,
  DisasterMembership,
  Observation,
  ObservationFeedback,
  ObservationPhoto,
  DisasterSetting,
];

export {
  Disaster,
  DisasterMembership,
  Observation,
  ObservationFeedback,
  ObservationPhoto,
  DisasterSetting,
};
